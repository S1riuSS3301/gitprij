# VDS_HUB - Server Rental Platform

Платформа для аренды виртуальных серверов (VDS/VPS) с полноценной системой управления, JWT аутентификацией и админ-панелью.

## Возможности

- 🔐 **JWT Аутентификация** с bcrypt хешированием паролей
- 👤 **Управление пользователями** с ролями (user/admin)
- 💰 **Управление тарифами** с редактированием цен в админ-панели
- 📊 **Панель управления** для пользователей
- 🗄️ **Локальная база данных** через localStorage (работает в браузере)
- 🎨 **Современный UI** с Tailwind CSS и shadcn/ui
- 🌐 **Полная русификация** интерфейса

## Технологии

- **Frontend**: Next.js 15, React 19, TypeScript
- **Styling**: Tailwind CSS v4, shadcn/ui
- **Backend**: Next.js API Routes, Server Actions
- **Database**: localStorage (mock database для браузера)
- **Authentication**: JWT с bcryptjs
- **Deployment**: Vercel

## Начало работы

### 1. Установка зависимостей

\`\`\`bash
npm install
\`\`\`

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env.local`:

\`\`\`bash
cp .env.example .env.local
\`\`\`

Отредактируйте `.env.local` и установите секретный ключ для JWT:

\`\`\`env
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
\`\`\`

### 3. Запуск в режиме разработки

\`\`\`bash
npm run dev
\`\`\`

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

База данных автоматически инициализируется при первом запуске с тестовыми тарифными планами.

## Структура проекта

\`\`\`
├── app/                      # Next.js App Router
│   ├── api/                  # API Routes
│   ├── auth/                 # Страницы аутентификации
│   ├── admin/                # Админ-панель
│   ├── dashboard/            # Панель пользователя
│   └── page.tsx              # Главная страница
├── components/               # React компоненты
│   ├── admin/                # Компоненты админ-панели
│   ├── auth/                 # Компоненты аутентификации
│   ├── dashboard/            # Компоненты панели управления
│   └── ui/                   # UI компоненты (shadcn/ui)
├── lib/                      # Утилиты и библиотеки
│   ├── actions/              # Server Actions
│   ├── auth-utils.ts         # Утилиты аутентификации
│   ├── jwt.ts                # JWT токены
│   └── db.ts                 # Mock database (localStorage)
└── middleware.ts             # Next.js Middleware для auth
\`\`\`

## Роли пользователей

### User (Пользователь)
- Просмотр и заказ серверов
- Управление своими заказами
- Редактирование профиля

### Admin (Администратор)
- Все возможности пользователя
- Управление тарифными планами
- Редактирование цен
- Просмотр всех пользователей и заказов
- Управление системой

## Регистрация и вход

Создайте новый аккаунт через страницу регистрации `/register`. Первый зарегистрированный пользователь автоматически получает роль администратора.

## База данных

Приложение использует localStorage для хранения данных в браузере. Данные автоматически инициализируются при первом запуске:

- **Тарифные планы**: Starter ($12/мес), Professional ($24/мес), Enterprise ($48/мес)
- **Пользователи**: Создаются при регистрации
- **Заказы**: Создаются при оформлении сервера

Все данные сохраняются локально в браузере и доступны между сессиями.

## Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения:
   - `JWT_SECRET` - Секретный ключ для JWT
   - `NEXT_PUBLIC_APP_URL` - URL вашего приложения
3. Деплой произойдет автоматически

## Безопасность

- JWT токены хранятся в httpOnly cookies
- Пароли хешируются с помощью bcryptjs (10 раундов)
- Middleware проверяет аутентификацию на защищенных маршрутах
- API routes защищены проверкой JWT токенов
- Данные хранятся локально в браузере пользователя

## Команды разработки

\`\`\`bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для production
npm run start        # Запуск production сервера
npm run lint         # Проверка кода
\`\`\`

## Лицензия

MIT
